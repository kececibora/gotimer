Fixed: on some phones the “OK” button in Live Settings was cut off below the screen and could not be tapped. The panel now scrolls and OK stays pinned at the bottom.

The clock screen and the time pickers also fit correctly on small screens and with large system font sizes.
