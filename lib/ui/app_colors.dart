import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Base
  static const Color bg = Color(0xFF181920); // kBgColor
  static const Color card = Color(0xFF22252F); // kCardColor
  static const Color textSecondary = Color(0xFFB2B5C3);

  // Timer areas
  static const Color active = Color(0xFFFFA851);
  static const Color blackBase = Color(0xFF111111);
  static const Color whiteBase = Color(0xFFF4F5FB);

  // Control bar
  static const Color controlBar = Color(0xFF202020);
  static const Color controlButton = Color(0xFF303030);

  // Common
  static const Color white = Colors.white;
  static const Color black = Colors.black;

  // Gradients
  static const Color gradientTop = Colors.black54; // background üst okunabilirlik
}
